# Hostel Management Multi-Agent System — Backend Dependencies
# Phase 0 (core scaffold)
fastapi==0.115.0
python-multipart==0.0.9  # required by FastAPI for Form() + File() upload endpoints
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
supabase==2.7.4
psycopg2-binary==2.9.9
pgvector==0.3.2
pydantic==2.8.2
slowapi==0.1.9          # rate limiting for face recognition endpoint

# Phase 1 (IRIS — Vision Agent)
# switched from insightface→deepface (insightface needs Visual C++ on Windows)
onnxruntime         # ONNX runtime (also used by deepface internally)
opencv-python==4.10.0.84
numpy==1.26.4
Pillow==10.4.0
deepface            # face recognition — no C++ compile needed, works on Windows
tf-keras            # deepface backend

# Phase 2 (SENTINEL — Attendance Agent)
apscheduler==3.10.4

# Phase 3B (NOURISH — Gemini menu parser)
# Uses httpx for Gemini REST API calls (avoids protobuf conflicts with TensorFlow)
httpx

# Phase 3C + 4 + 5 (NOURISH NLP + FIXR + HERALD — Groq)
# groq==0.9.0

# Phase 6 (Auth — JWT verification)
# python-jose[cryptography]==3.3.0
